# ───── General OS / Editor clutter ───────────────────────────────
.DS_Store
*.swp
*.swo
*.sublime-workspace
.idea/
.vscode/
.venv/
venv/
.venv_pack/

# Build artifacts and packaging
api_lambda_build/
terraform/api_lambda.zip
api_lambda.zip

# ───── Python artifacts ──────────────────────────────────────────
# Virtual envs
venv/
.env/
# Byte-code & cache
__pycache__/
*.py[cod]
*.pyo
*.pyd
# Distribution archives
*.egg-info/
dist/
build/

# ───── Jupyter / Notebook checkpoints (if any) ───────────────────
.ipynb_checkpoints/

# ───── Terraform artifacts ───────────────────────────────────────
.terraform/
terraform.tfstate
terraform.tfstate.*
crash.log
# Local plan files
*.tfplan
*.tfbackup

# ───── Lambda ZIPs & build outputs ───────────────────────────────
lambda_function.zip
*.zip

# ───── Data feed & test artefacts ────────────────────────────────
data_feed/*.json
test/*.json
*.csv
*.parquet

# ───── Logs & debug output ───────────────────────────────────────
*.log

# ───── MacOS extended attributes ─────────────────────────────────
.AppleDouble
.LSOverride
Icon?

# Terraform state and plan files (if accidentally generated locally)
terraform/terraform.tfstate*
terraform/*.tfplan*